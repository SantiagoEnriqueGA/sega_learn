[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sega_learn"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages]
find = { include = ["sega_learn*"] }  # This ensures only sega_learn is installed
